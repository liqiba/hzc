services:
  hetzner-traffic-guard:
    build: .
    container_name: hetzner-traffic-guard
    restart: unless-stopped
    ports:
      - "1227:1227"
    env_file:
      - .env
    volumes:
      - ./state:/app/state
